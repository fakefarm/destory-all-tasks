.nav-bar
  .l-constrained
    %section.nav-logo
      %h1 Destroy All Tasks
      %h2
        Inbox Zero for Tasks
        =link_to "(by Dave)", 'http://www.hireDave.me'

    %section.nav-links
      %ul
        %li
          - if current_user
            = link_to "Log out", logout_path
          - else
            =link_to "Log In", login_path
        %li
          - if current_user
            = link_to 'Tasks', tasks_path
          - else
            =link_to 'Sign up', signup_path
