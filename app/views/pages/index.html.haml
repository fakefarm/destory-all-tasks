.summary
  %h1 Inbox zero for tasks
  %h2 By Destroy all Tasks

  .user-header
    - if current_user
      %p.home-page
        Logged in as
        = current_user.email
        = link_to "Log out", logout_path
    - else
      %p.home-page=link_to "Log In", login_path
