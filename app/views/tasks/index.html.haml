%h1 Listing tasks

%table
  %tr
    %th Item
    %th

  - @tasks.each do |task|
    %tr
      %td= link_to task.item, edit_task_path(task)
      %td= link_to 'Destroy', task, :method => :delete

%br

= render 'form'