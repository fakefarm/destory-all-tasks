= form_for @task do |f|
  - if @task.errors.any?
    #error_explanation
      %h2= "#{pluralize(@task.errors.count, "error")} prohibited this task from being saved:"
      %ul
        - @task.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.text_field :item, placeholder: 'Task to destroy!'
  .field
    = f.text_field :tags, placeholder: 'tags'
  .actions
    = f.submit 'Save'
