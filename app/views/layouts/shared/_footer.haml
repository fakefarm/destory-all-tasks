- flash.each do |key, value|
  .alert{class: "alert-#{key}"}
    = value

.footer
  %ul
    %li
      %a{href: "#{root_path}"} Home
    - if current_user
      %li
        %a{href: "#{}"} Tasks
      %li
        %a{href: "#{logout_path}"} Log out
    - else
      %li
        %a{href: "#{login_path}"} Log In
    %li
      %a{href: "http://www.hireDave.me"} made by hireDave
    / %li
    /   %a{href: "#{style_guide_path}"} Style Guide
    / %li
    /   %a{href: "#{overview_path}"} Overview
