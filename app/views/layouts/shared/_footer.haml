- flash.each do |key, value|
  .alert{class: "alert-#{key}"}
    = value

.footer
  = link_to 'Home', root_path
  = link_to 'Tasks', tasks_path
  = mail_to 'dave@hireDave.me', 'Ideas?'
  |
  - if current_user
    = link_to "Log out", logout_path
  - else
    =link_to "Log In", login_path
