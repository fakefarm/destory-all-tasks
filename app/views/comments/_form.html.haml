= form_for @comment do |f|
  = f.hidden_field :task_id, value: @task.id
  .field
    .comment-field= f.text_area :entry, rows: "5", placeholder: "Jot some notes."
  .actions.save-btn
    = f.submit 'Save', class: 'link-button-dark'
