.summary
  %table
    %tr
      %th User
      %th Number of tasks
      %th Last Task
    - @users.each do |user|
      %tr
        %td= user.email
        %td= user.tasks.count
        %td= user.tasks.last.created_at.to_formatted_s(:short) unless user.tasks.last.nil?
